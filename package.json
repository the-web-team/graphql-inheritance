{
  "name": "graphql-inheritance-js",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:the-web-team/graphql-inheritance-js.git",
  "author": "Vinlock <dak@pvpallday.com>",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "prebuild": "rm -rf dist/*",
    "build": "babel -d ./dist ./lib --source-maps inline --ignore '**/*.test.js' --copy-files",
    "docs": "rm -rf ./docs/* && jsdoc -c ./.jsdoc.json",
    "test": "jest",
    "deploy": "yarn publish && yarn push"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "eslint-config-airbnb": "^17.1.1",
    "jest": "^24.8.0",
    "jsdoc": "^3.6.3"
  }
}
